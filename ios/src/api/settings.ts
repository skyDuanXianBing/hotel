import * as roomGroupApi from '@/api/roomGroup'
import * as sortConfigApi from '@/api/sortConfig'
import * as paymentMethodApi from '@/api/paymentMethod'
import * as noteCategoryApi from '@/api/noteCategory'

export const settingsApi = Object.freeze({
  roomGroupApi,
  sortConfigApi,
  paymentMethodApi,
  noteCategoryApi,
})

export default settingsApi
